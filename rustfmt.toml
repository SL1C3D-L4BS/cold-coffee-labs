# Greywater Rust formatting (BLD crate only) — per docs/10 §6.8.
edition = "2021"
max_width = 100
use_small_heuristics = "Max"
imports_granularity = "Module"
reorder_imports = true
reorder_modules = true
newline_style = "Unix"
