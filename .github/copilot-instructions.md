# GitHub Copilot — repository notes

- C++23 engine, Vulkan rendering, CMake presets under `CMakePresets.json`.
- Prefer matching existing style, includes, and naming in touched directories.
- Run the relevant `ctest` preset after non-trivial changes when feasible.
