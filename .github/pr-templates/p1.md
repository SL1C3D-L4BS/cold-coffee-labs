# PR #1 — Phase 20.5 + Phase 21 (stacked, base: `main`)

**Head:** `pivot/phase-21-p1` (tip `4c77e14`)

## Contents

- Wire-up audit completion + prompt pack (`f075598` … `f491dec`)
- AI-tooling **scaffold** (`ec8f713`) and Phase 20 verify + P21 weeks through **Hell Frame** exit
- P20 post / perf / `sandbox_hell_frame` as described in `docs/02` Phase 21 retrospective

## After merge

PR #2 (`pivot/phase-21-22-execution`) contains Phase 22 + full real AI-tooling
install; it will **auto-shrink** on GitHub once this branch merges to `main`.

## Test / review notes

- Build presets: `CMakePresets.json` (AgentShield: reference ADR if touching presets).
- Graphify: `graphify-out/GRAPH_REPORT.md` is the pre-search index per ADR-0118.
