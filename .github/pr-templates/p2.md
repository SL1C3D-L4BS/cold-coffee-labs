# PR #2 — Phase 22 + full AI-tooling install (stacked, base: `main`)

**Head:** `pivot/phase-21-22-execution` (includes C0–C7 finish-line work)

## Contents

- **Gameplay / engine:** P22 W143–W148 (`0d188f7` and related) + `docs/02` tick/retros
- **AI tooling (real):** ADR-0118/0119/0120, graphify 0.4.31 + committed `graphify-out/`, ECC
  v1.10.0 vendored + full Cursor/global install, 7 MCP servers, AgentShield
  hard-fail, `install_ecc` / `uninstall_ecc`, `ecc_dashboard.py`, `AGENTS.md` refresh

## Prerequisite

Prefer merging **PR #1** first so this diff is review-sized; if base is
out of date, rebase or merge `main` after PR #1 lands.

## Policy

- **Penpax:** still waitlist-only; no integration in this PR.
