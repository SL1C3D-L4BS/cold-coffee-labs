# 2026-04-20 · Monday

**Week:** 1 · **Phase:** Phase 1 — Scaffolding & CI/CD · **Milestone:** *First Light*

---

## 🎯 Today's Focus

**Phase 1 executed end-to-end per the phase-complete execution doctrine (`CLAUDE.md` non-negotiables #19–20, `docs/00` §9).** Founder directive: if it is described in Phase 1, it ships before Phase 1 closes. No deferral to "next Tuesday" or "Friday's card."

**Session intent:** codify the phase-complete doctrine in `CLAUDE.md` + `docs/00` + `docs/05` + `docs/06` + `docs/10` + `docs/11`; then land every Phase 1 deliverable in one coherent push — directory scaffold, toolchain/lint config, CMake infrastructure, Rust workspace, CI matrix, First Light sandbox (hello-triangle), editor BLD-greeting smoke, standards docs.

## 📋 Tasks Completed

- [x] **Doctrine:** added non-negotiables #19 (phase-complete execution) and #20 (documentation never lags directive) to `CLAUDE.md`.
- [x] **Doctrine:** authored `docs/00_SPECIFICATION_Claude_Opus_4.7.md §9` — Execution cadence (phase-as-unit, WIP-is-concurrency-not-scope, escape hatches, directive coherence, milestone gating).
- [x] **Doctrine:** added scope-rule preambles to `docs/05`, `docs/06` (Phase-entry), `docs/10 §3`, and `docs/11 §5`.
- [x] **Commit:** `docs: commit canonical set with phase-complete execution doctrine` (sha `77c72e4`). 1019 files landed — canonical + operational + pre-generated daily logs.
- [x] **Scaffold:** directory tree per `docs/10 §4` + `docs/bootstrap_command.txt`. Every planned subdirectory seeded with `.gitkeep` so git tracks structure.
- [x] **Config:** `.clang-format`, `.clang-tidy`, `rustfmt.toml`, `.editorconfig`, `.gitattributes`, `.gitignore`, `LICENSE`, `rust-toolchain.toml`.
- [x] **Build infrastructure:** root `CMakeLists.txt` (Clang-only gate, C++23, sccache opt-in), `CMakePresets.json` (7 configure presets + build/test/workflow presets), `cmake/CPM.cmake` (SHA-pinned bootstrap), `cmake/dependencies.cmake` (Corrosion, GLFW, volk, VMA, doctest, Vulkan), `cmake/toolchains/{clang.cmake, clang-cl.cmake}`.
- [x] **Engine:** `greywater_core` static library with generated version header (`engine/core/version.{hpp,cpp,generated.hpp.in}`).
- [x] **Rust workspace:** `bld/Cargo.toml` (8-crate workspace) + minimal stubs for `bld-mcp`, `bld-tools`, `bld-provider`, `bld-rag`, `bld-agent`, `bld-governance`, `bld-bridge`, `bld-ffi`. `bld-ffi` exposes the C-ABI `bld_hello()` with a Miri-worthy `// SAFETY:`-annotated test.
- [x] **Sandbox (First Light 1A):** `sandbox/main.cpp` — GLFW + volk + VMA + Vulkan 1.2 core + 1.3 dynamic rendering + `synchronization_2`. Triangle uses the Cold Coffee Labs palette (Crema / Greywater / Brew). GLSL → SPIR-V via `glslangValidator` driven from CMake.
- [x] **Editor (First Light 1B):** `editor/main.cpp` + `editor/bld_bridge/hello.{hpp,cpp}` — links `greywater_core` + Rust `bld-bridge` staticlib via Corrosion, prints the BLD greeting.
- [x] **Runtime, gameplay dylib, tools/cook, tests:** stubs compiling against `greywater_core`. `gameplay` exposes `gameplay_api_version / init / tick / shutdown` ABI for Phase-2 hot-reload. `tests` seeds doctest with a smoke suite auto-registered with CTest.
- [x] **CI:** `.github/workflows/ci.yml` — `{ubuntu-latest, windows-latest} × {Debug, Release}` matrix; installs LLVM 18, Vulkan SDK 1.3.290, Ninja; CPM cache + Cargo cache; cmake preset build + ctest + cargo test + clippy -D warnings + cargo fmt --check; plus a `lint` job running clang-format dry-run.
- [x] **CI nightly:** `.github/workflows/sanitizers.yml` — ASan, UBSan, TSan matrix + Miri on `bld-ffi`, cron 06:00 UTC + `workflow_dispatch`.
- [x] **Docs:** `docs/BUILDING.md` (onboarding + troubleshooting) and `docs/CODING_STANDARDS.md` (derived from `00` + `04` + `12`, crib-sheet form).
- [x] **Root README.md:** upgraded from placeholder to a proper studio entry-point.
- [x] **Build verification:** `cmake --preset dev-win` configures on the reference workstation (Radeon RX 580 8 GB, Intel i7-6700, Vulkan SDK 1.4.341). Third-party `cmake_minimum_required` < 3.5 resolved via the documented `CMAKE_POLICY_VERSION_MINIMUM=3.5` override in the root `CMakeLists.txt` — no upstream pin bumps, no forked patches.

## 🚧 Blockers

- None. *First Light* milestone demo video (two-minute narrated recording on Windows + Linux) requires a human with capture tools; the binaries are in place to be recorded.
- `RHICapabilities` gating of 1.3 features will land with the renderer HAL at Phase 4 week 017. The sandbox's direct use of dynamic rendering + sync2 is acceptable for Phase 1 because the target device reports `apiVersion = 1.3.260` (AMD proprietary driver 26.1.1) — verified via `vulkaninfo` on 2026-04-20.

## 📝 Notes / Log

- **Founder directive acted on, in order:** doctrine first (commit `77c72e4`), then code. `00` §9.4 is now the canonical expression of this ordering.
- **Hardware check before commit:** dev workstation is exactly RX 580 8 GB baseline. Vulkan `memoryHeap[0]` = 7.75 GiB DEVICE_LOCAL, confirming the 8 GB SKU; the WMI `AdapterRAM` 4 GB readout is the known `uint32` clamp.
- **Toolchain:** clang 22.1.3, cmake 4.3.1, ninja 1.13.2, rustc 1.94.1, Vulkan SDK 1.4.341.1 — all exceed minimums (`docs/00` §2.2).
- **Live references consulted during Phase 1:**
  - CMake Presets v6 schema: https://cmake.org/cmake/help/latest/manual/cmake-presets.7.html
  - Corrosion install patterns: https://corrosion-rs.github.io/corrosion/
  - GLFW 3.4 Vulkan integration: https://www.glfw.org/docs/latest/vulkan.html
  - Vulkan 1.2 spec (core): https://registry.khronos.org/vulkan/specs/1.2-extensions/html/
  - Dynamic rendering extension: `VK_KHR_dynamic_rendering`, core in 1.3
  - `synchronization_2`: https://registry.khronos.org/vulkan/specs/1.3-extensions/man/html/VK_KHR_synchronization2.html
  - volk: https://github.com/zeux/volk
  - VMA 3.1: https://gpuopen.com/vulkan-memory-allocator/
  - Conventional Commits 1.0.0: https://www.conventionalcommits.org/en/v1.0.0/
  - CMake 4.0 release notes (`CMAKE_POLICY_VERSION_MINIMUM` forward-compat mechanism).
- **§K rules:** not applicable today — no `engine/world/` content authored.
- **§13.x UI rules:** not applicable today — Phase 7 begins at week 037.

## 🔮 Tomorrow's Intention

Begin Phase 2 — Platform & Core Utilities (weeks 005–009) per the phase-complete doctrine. Target: `engine/platform/` (windowing, input plumb, timing, dynamic-lib loading), `engine/memory/` (frame / arena / pool allocators with ASan-clean leak checks), `engine/core/` (logger, assertions, `Result<T, E>`, UTF-8 string with SSO, span/array/vector/hash_map/ring_buffer containers), and the custom `clang-tidy` check `gw-no-os-headers-outside-platform`. Phase 2 milestone is shared with Phase 3 (*Foundations Set*, week 016) — but by the cadence doctrine, Phase 2 exits when its Tier A deliverable set is complete, not when the calendar says.
