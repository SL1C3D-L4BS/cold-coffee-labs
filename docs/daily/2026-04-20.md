# 2026-04-20 · Monday

**Week:** 1 · **Phase:** Phase 7 — Editor Foundation · **Milestone:** *Editor v0.1*

> Note: Phases 1–6 all landed during this same Monday (see git log `77c72e4 → cc7dc94`).
> This file is therefore a multi-session day-log. The historical record below (Phase 1
> scaffolding through *First Light*) is retained verbatim. The new section
> "Afternoon session — Phase 7 unblock + audit" captures today's later work.

---

## 🎯 Today's Focus

**Phase 1 executed end-to-end per the phase-complete execution doctrine (`CLAUDE.md` non-negotiables #19–20, `docs/00` §9).** Founder directive: if it is described in Phase 1, it ships before Phase 1 closes. No deferral to "next Tuesday" or "Friday's card."

**Session intent:** codify the phase-complete doctrine in `CLAUDE.md` + `docs/00` + `docs/05` + `docs/06` + `docs/10` + `docs/11`; then land every Phase 1 deliverable in one coherent push — directory scaffold, toolchain/lint config, CMake infrastructure, Rust workspace, CI matrix, First Light sandbox (hello-triangle), editor BLD-greeting smoke, standards docs.

## 📋 Tasks Completed

- [x] **Doctrine:** added non-negotiables #19 (phase-complete execution) and #20 (documentation never lags directive) to `CLAUDE.md`.
- [x] **Doctrine:** authored `docs/00_SPECIFICATION_Claude_Opus_4.7.md §9` — Execution cadence (phase-as-unit, WIP-is-concurrency-not-scope, escape hatches, directive coherence, milestone gating).
- [x] **Doctrine:** added scope-rule preambles to `docs/05`, `docs/06` (Phase-entry), `docs/10 §3`, and `docs/11 §5`.
- [x] **Commit:** `docs: commit canonical set with phase-complete execution doctrine` (sha `77c72e4`). 1019 files landed — canonical + operational + pre-generated daily logs.
- [x] **Scaffold:** directory tree per `docs/10 §4` + `docs/bootstrap_command.txt`. Every planned subdirectory seeded with `.gitkeep` so git tracks structure.
- [x] **Config:** `.clang-format`, `.clang-tidy`, `rustfmt.toml`, `.editorconfig`, `.gitattributes`, `.gitignore`, `LICENSE`, `rust-toolchain.toml`.
- [x] **Build infrastructure:** root `CMakeLists.txt` (Clang-only gate, C++23, sccache opt-in), `CMakePresets.json` (7 configure presets + build/test/workflow presets), `cmake/CPM.cmake` (SHA-pinned bootstrap), `cmake/dependencies.cmake` (Corrosion, GLFW, volk, VMA, doctest, Vulkan), `cmake/toolchains/{clang.cmake, clang-cl.cmake}`.
- [x] **Engine:** `greywater_core` static library with generated version header (`engine/core/version.{hpp,cpp,generated.hpp.in}`).
- [x] **Rust workspace:** `bld/Cargo.toml` (8-crate workspace) + minimal stubs for `bld-mcp`, `bld-tools`, `bld-provider`, `bld-rag`, `bld-agent`, `bld-governance`, `bld-bridge`, `bld-ffi`. `bld-ffi` exposes the C-ABI `bld_hello()` with a Miri-worthy `// SAFETY:`-annotated test.
- [x] **Sandbox (First Light 1A):** `sandbox/main.cpp` — GLFW + volk + VMA + Vulkan 1.2 core + 1.3 dynamic rendering + `synchronization_2`. Triangle uses the Cold Coffee Labs palette (Crema / Greywater / Brew). GLSL → SPIR-V via `glslangValidator` driven from CMake.
- [x] **Editor (First Light 1B):** `editor/main.cpp` + `editor/bld_bridge/hello.{hpp,cpp}` — links `greywater_core` + Rust `bld-bridge` staticlib via Corrosion, prints the BLD greeting.
- [x] **Runtime, gameplay dylib, tools/cook, tests:** stubs compiling against `greywater_core`. `gameplay` exposes `gameplay_api_version / init / tick / shutdown` ABI for Phase-2 hot-reload. `tests` seeds doctest with a smoke suite auto-registered with CTest.
- [x] **CI:** `.github/workflows/ci.yml` — `{ubuntu-latest, windows-latest} × {Debug, Release}` matrix; installs LLVM 18, Vulkan SDK 1.3.290, Ninja; CPM cache + Cargo cache; cmake preset build + ctest + cargo test + clippy -D warnings + cargo fmt --check; plus a `lint` job running clang-format dry-run.
- [x] **CI nightly:** `.github/workflows/sanitizers.yml` — ASan, UBSan, TSan matrix + Miri on `bld-ffi`, cron 06:00 UTC + `workflow_dispatch`.
- [x] **Docs:** `docs/BUILDING.md` (onboarding + troubleshooting) and `docs/CODING_STANDARDS.md` (derived from `00` + `04` + `12`, crib-sheet form).
- [x] **Root README.md:** upgraded from placeholder to a proper studio entry-point.
- [x] **Build verification:** `cmake --preset dev-win` configures on the reference workstation (Radeon RX 580 8 GB, Intel i7-6700, Vulkan SDK 1.4.341). Third-party `cmake_minimum_required` < 3.5 resolved via the documented `CMAKE_POLICY_VERSION_MINIMUM=3.5` override in the root `CMakeLists.txt` — no upstream pin bumps, no forked patches.

## 🚧 Blockers

- None. *First Light* milestone demo video (two-minute narrated recording on Windows + Linux) requires a human with capture tools; the binaries are in place to be recorded.
- `RHICapabilities` gating of 1.3 features will land with the renderer HAL at Phase 4 week 017. The sandbox's direct use of dynamic rendering + sync2 is acceptable for Phase 1 because the target device reports `apiVersion = 1.3.260` (AMD proprietary driver 26.1.1) — verified via `vulkaninfo` on 2026-04-20.

## 📝 Notes / Log

- **Founder directive acted on, in order:** doctrine first (commit `77c72e4`), then code. `00` §9.4 is now the canonical expression of this ordering.
- **Hardware check before commit:** dev workstation is exactly RX 580 8 GB baseline. Vulkan `memoryHeap[0]` = 7.75 GiB DEVICE_LOCAL, confirming the 8 GB SKU; the WMI `AdapterRAM` 4 GB readout is the known `uint32` clamp.
- **Toolchain:** clang 22.1.3, cmake 4.3.1, ninja 1.13.2, rustc 1.94.1, Vulkan SDK 1.4.341.1 — all exceed minimums (`docs/00` §2.2).
- **Live references consulted during Phase 1:**
  - CMake Presets v6 schema: https://cmake.org/cmake/help/latest/manual/cmake-presets.7.html
  - Corrosion install patterns: https://corrosion-rs.github.io/corrosion/
  - GLFW 3.4 Vulkan integration: https://www.glfw.org/docs/latest/vulkan.html
  - Vulkan 1.2 spec (core): https://registry.khronos.org/vulkan/specs/1.2-extensions/html/
  - Dynamic rendering extension: `VK_KHR_dynamic_rendering`, core in 1.3
  - `synchronization_2`: https://registry.khronos.org/vulkan/specs/1.3-extensions/man/html/VK_KHR_synchronization2.html
  - volk: https://github.com/zeux/volk
  - VMA 3.1: https://gpuopen.com/vulkan-memory-allocator/
  - Conventional Commits 1.0.0: https://www.conventionalcommits.org/en/v1.0.0/
  - CMake 4.0 release notes (`CMAKE_POLICY_VERSION_MINIMUM` forward-compat mechanism).
- **§K rules:** not applicable today — no `engine/world/` content authored.
- **§13.x UI rules:** not applicable today — Phase 7 begins at week 037.

## 🔮 Tomorrow's Intention (superseded by the afternoon session below)

**ARCHIVED — this block was filed in the morning and reflected the Phase 5 state of play; see the "Afternoon session" block and `docs/AUDIT_MAP_2026-04-20.md` for the current state and the real next-day plan.**

---

## Afternoon session — Phase 7 unblock + audit (Monday 2026-04-20, later)

**Session intent:** the editor crashed at startup with an ImGui font-atlas assertion, docs drifted ~2 phases behind git, and several Phase 7 deliverables were still stubs. This session unblocks the editor, syncs the docs, and runs a read-only CLAUDE.md audit against every non-test directory.

### Diagnostics up front
- `cmake --preset dev-win` + `cmake --build --preset dev-win` — green.
- `ctest --preset dev-win` — 72/72 pass.
- `gw_sandbox.exe` — runs, renders, initializes Vulkan.
- `gw_runtime.exe` — intentional stub, runs.
- `gw_editor.exe` — **crashed** at `atlas->TexIsBuilt` assertion in `imgui_draw.cpp` (ImGui 1.92 RendererHasTextures API requires a Vulkan backend to upload the font atlas; the editor had Vulkan init stubbed).

### Phase A — unblock
- [x] Rewrote `editor/app/editor_app.cpp` init path: real `volkInitialize()` → instance (with debug utils in Debug, validation layer opt-in via new `GW_VK_VALIDATION` define) → surface → physical-device pick → device with `VK_KHR_SWAPCHAIN`, `VK_KHR_DYNAMIC_RENDERING`, `VK_KHR_SYNCHRONIZATION_2` + the corresponding feature structs → queues → swapchain → command pool & buffers → per-frame sync primitives → ImGui descriptor pool.
- [x] Wrote real `recreate_swapchain()` with correct `oldSwapchain` handling, per-image render-finished semaphores, and `ImGui_ImplVulkan_SetMinImageCount` sync.
- [x] Wired `ImGui_ImplVulkan_Init` against the new (2025/09/26+) `ImGui_ImplVulkan_InitInfo` shape: `PipelineInfoMain` + `PipelineInfoForViewports` carry MSAA + dynamic-rendering format info. `vk_->imgui_color_format` lives on the backend so ImGui's internal pipeline-rebuild picks up swapchain-format changes.
- [x] Implemented `begin_frame` / `end_frame` with explicit `VkImageMemoryBarrier2` UNDEFINED→COLOR_ATTACHMENT_OPTIMAL→PRESENT_SRC transitions, `vkCmdBeginRendering`/`vkCmdEndRendering` dynamic rendering, `vkQueueSubmit2`, and multi-viewport update/render.
- [x] Fixed `MinImageCount < 2` assertion by clamping `swapchain_min_count = max(caps.minImageCount, 2u)` (some drivers report 1 even though we allocate ≥ 2).
- [x] Pimpl'd all raw Vulkan state behind `struct EditorVulkanBackend` so `editor_app.hpp` no longer needs `<volk.h>`.
- [x] Removed raw `new` in `EditorApplication` ctor — panels are now constructed with `std::make_unique` (CLAUDE.md non-negotiable #5).
- [x] Dropped the `VulkanDevice&` dependency from `ViewportPanel` (the scene-image allocation is a Phase 8 task; the Phase 7 panel no longer pretends to own a device reference it can't use).
- [x] Added `$<$<CONFIG:Debug>:GW_VK_VALIDATION=1>` to `editor/CMakeLists.txt` so the debug-utils messenger actually runs in Debug builds.
- [x] Verified: build green; `gw_editor.exe` launches, opens the docked window, no validation errors, no ImGui asserts.

### Phase A — docs sync
- [x] `docs/05_ROADMAP_AND_MILESTONES.md` phase table: Phase 5 + 6 marked **completed**, Phase 7 marked **in_progress**.
- [x] *Foundation Renderer* milestone block updated to cite commit `b191ca4` as engineering-complete (recording gate still pending per §0 rule).
- [x] *Editor v0.1* milestone block updated: Phase 7 opened, scaffolding commit `cc7dc94`, editor boots cleanly against ImGui 1.92 + Vulkan 1.3 dynamic rendering. Remaining items enumerated.
- [x] `docs/README.md` row for 05 corrected: "24 build phases + LTS Sustenance (Phase 25), 162-week schedule" (was "18-phase plan + LTS Sustenance, 125-week schedule").
- [x] This file: header phase field corrected to Phase 7 + explanatory note preserved the historical Phase 1 body above.

### Phase C — read-only CLAUDE.md audit (parallel, 4 agents)
Four read-only audit agents ran against `engine/`+`bld/`, `editor/`+`runtime/`+`sandbox/`+`gameplay/`+`tools/`, the CMake/Corrosion build system, and `docs/`. All findings are catalogued in **`docs/AUDIT_MAP_2026-04-20.md`** — the canonical operational snapshot (supersedes the abbreviated bullet list that was originally here). See that file for the P0/P1/P2/P3 breakdown, resolution status, and recommended application order.

---

## Evening session — Round 1 audit fixes (Monday 2026-04-20, night)

**Session intent:** work `docs/AUDIT_MAP_2026-04-20.md` §7 Round 1 (pre-Phase-8 entry items) to completion. Build and ctest must stay green after each fix.

### Fixes landed
- **P0-1 (frame-graph submit UB).** `engine/render/frame_graph/submit.cpp` — hoisted `wait_infos` / `signal_infos` / `cmd_submit_infos` to outer-scope containers indexed by submission so `VkSubmitInfo2` pointers stay live until `vkQueueSubmit2` returns.
- **P0-2 (timeline semaphore counter misuse).** Same file + `submit.hpp` — split `get_next_value` into `current_value(queue_type)` (pure read, used for waits) and `reserve_next_signal_value(queue_type)` (mutating, only when a signal is actually emitted). Old name kept as `[[deprecated]]` for one cycle. `build_submissions` / `add_cross_queue_dependencies` / `wait_for_completion` all updated to use the right one.
- **P0-3 (swapchain `maxImageCount == 0`).** `engine/render/hal/vulkan_swapchain.cpp` — `(maxImageCount == 0) ? desired : min(desired, maxImageCount)` per spec.
- **P0-4 (VulkanMemoryManager dead code).** Retired `engine/render/hal/vulkan_memory.{hpp,cpp}` entirely. `VulkanDevice::init_vma()` remains the single source of truth for VMA. Also deleted the dead duplicate `engine/render/hal/CMakeLists.txt` (was never `add_subdirectory`-ed).
- **P0-5 (duplicate `ImGuizmo::BeginFrame`).** Removed the second call in `editor/panels/viewport_panel.cpp`; now called exactly once per frame in `EditorApplication::begin_frame`.
- **P0-6 (gizmo hard-coded orthographic flag).** `editor/viewport/gizmo_system.{hpp,cpp}` — threaded `bool ortho` through `GizmoSystem::draw(...)`; viewport panel passes `camera_.mode() == CameraMode::Ortho`.
- **P1-3 (instance extension list named device-only exts).** `engine/render/hal/vulkan_instance.cpp` — removed the misspelled `VK_EXT_dynamic_rendering` and `VK_KHR_synchronization2` (both are device extensions); clarifying comment added. `VK_EXT_descriptor_indexing` also removed from the instance list (it's device-level too).
- **P1-5 (PIE hot-reload missing re-init).** `editor/pie/gameplay_host.{hpp,cpp}` — stores `GameplayContext*` on `enter_play`; `reload_if_changed` re-calls `gw_gameplay_init` after the DLL reloads. On reload failure the host drops back to `PIEState::Editor` instead of leaving a broken DLL staged.
- **P1-7 (rustfmt edition mismatch).** `rustfmt.toml` → `edition = "2024"` to match the `bld/` workspace.
- **P1-9 (Vulkan SDK pin drift).** `.github/workflows/ci.yml` + `sanitizers.yml` pinned to `1.4.341.1`; `docs/BUILDING.md` line updated.
- **P1-10 (duplicate `configure_file` / `target_sources`).** `engine/CMakeLists.txt` deduplicated.
- **P2-4 (OS headers in `gameplay_host.cpp`).** Rewrote to use `gw::platform::DynamicLibrary` (no more `<windows.h>` / `<dlfcn.h>`) and `gw::platform::FileSystem::copy_file_overwrite` / `last_write_stamp` (added to `engine/platform/fs.{hpp,cpp}` as part of this fix).
- **P2-5 (wrong non-negotiable citation).** `editor/pie/gameplay_host.cpp` + `editor/reflect/widget_drawer.cpp` — comments now cite `docs/12 §B2` (assert doctrine) instead of the mis-referenced `CLAUDE.md non-negotiable #8` (`string_view` to C APIs).
- **P2-9 (raw `delete` in DLL unload).** Now a by-product of the P2-4 fix: `lib_handle_` is `std::unique_ptr<gw::platform::DynamicLibrary>`; `unload_dll()` is `lib_handle_.reset()`.
- **P2-13 (`.clang-format` still C++20).** → `Standard: c++23`.
- **P3-1 (`[[nodiscard]]` on `Result<T>`).** `engine/render/frame_graph/error.hpp` — verified that MSVC STL's `std::expected` carries `[[nodiscard]]` on the type itself (warnings propagate through the `using` alias — the test suite now emits fresh warnings on legacy test code that discards results, which is intended). Attempted wrapping in a derived `[[nodiscard]] class` was reverted because `std::expected::and_then` requires the return type to be a specialization of `std::expected` (MSVC STL concept check rejects subclasses); documented the limitation in a comment at the alias site.
- **Doc sync (D-3, D-4, D-5, D-6, D-7, D-8).** `docs/README.md` "29-month" → "~37-month (162-week)"; this file's stale "Tomorrow's Intention" block archived; afternoon audit bullet list replaced with a pointer to `docs/AUDIT_MAP_2026-04-20.md`; `docs/05` *Foundations Set* sign-off redated 2026-04-20 (provisional); `CLAUDE.md :67` cross-ref split between §2 (#1–18) and §9 (#19–20); `docs/02:19` phase range fixed to 1–25.

### Verification
- `cmake --build --preset dev-win` — **green** after each fix.
- `ctest --preset dev-win --output-on-failure` — **72 / 72 pass**.
- Working tree is unstaged; no commit was made (commits are only on user request per project convention).

### Outstanding from Round 1 (deferred to next session)
- **P1-1** `RHICapabilities` layer (L) — the structural fix for "1.3 features are unconditional". This is the actual Phase-8 entry gate and needs a short ADR.
- **P2-1 / P2-2** `std::thread` → `engine/jobs/` migrations in `engine/assets/asset_db.cpp` and `tools/cook/cook_worker.cpp`. Requires extending `jobs::Scheduler` with a long-lived worker reservation (`reserve_worker(JobFn)`) — small API, but cross-subsystem touch.
- **P2-3** `engine/assets/vfs/watcher.cpp` — needs `engine/platform/file_watch.{hpp,cpp}` abstraction (ReadDirectoryChangesW / inotify split out of the VFS layer).
- **Round 2 items** (P1-6 glTF sparse + material cook, P2-11 editor DPI handling, P2-6 frame-graph STL removal, P2-7 world-space `float` → `float64` audit).

---

## Night session — Round 1 tail (Monday 2026-04-20, late-night)

**Session intent:** close the Round-1 tail flagged above (P1-1, P2-1, P2-2, P2-3). Doctrine precedes code per NN #20.

### Fixes landed
- **ADR-0003 `RHICapabilities`.** New `docs/adr/0003-rhi-capabilities.md` — context (unconditional 1.3 enablement), decision (probe-then-enable), consequences (baseline Tier-A fail-fast, Tier-G opportunistic branch). Accepted before any code change.
- **P1-1 `RHICapabilities` layer.**
  - Added `engine/render/hal/capabilities.{hpp,cpp}` — `RHICapabilities` struct (`timeline_semaphores`, `buffer_device_address`, `descriptor_indexing`, `dynamic_rendering`, `synchronization_2`, `mesh_shaders`, `ray_tracing_pipeline`, `ray_query`, `gpu_work_graphs`, `ext_memory_budget`, `total_device_local_bytes`, `max_descriptor_indexed_array_size`, `api_version`) and `probe_capabilities(VkPhysicalDevice)` free function. Convenience queries (`has_sync2()`, `has_dynamic_rendering()`, etc.) inline.
  - `VulkanDevice` runs `probe_capabilities` early; enforces Tier-A baseline (missing `timeline_semaphores` or `descriptor_indexing` → `std::runtime_error`); only chains 1.3 opportunistic feature structs (`VkPhysicalDeviceSynchronization2Features`, `VkPhysicalDeviceDynamicRenderingFeatures`) and their matching device extensions when the probe says yes. `capabilities()` accessor exposed to callers.
  - `FrameGraph::execute_pass` guards `vkCmdPipelineBarrier2` on `device.capabilities().has_sync2()`. Legacy sync1 / render-pass path still pending — tracked as Tier-G follow-up per ADR §3 "Short term".
  - `engine/render/CMakeLists.txt` picks up the new translation unit.
- **`engine/jobs/reserved_worker.hpp`** — new managed `std::thread` wrapper. Move-only, RAII-joins on destruction, idempotent `join()`. `Scheduler::reserve_worker(Fn)` factory exposed as a `static` so subsystems outside the `Scheduler`'s lifetime can still use it. Centralises all raw-`std::thread` ownership in `engine/jobs/` per NN #10.
- **P2-1 (asset_db loader thread).** `engine/assets/asset_db.{hpp,cpp}` — `loader_thread_` is now `gw::jobs::ReservedWorker`. Spawn via `gw::jobs::Scheduler::reserve_worker([this]() { loader_thread_fn(); })`. Shutdown still flips `stop_loader_`; RAII join on destruction. `TODO(Phase 10)` comment removed.
- **P2-2 (cook worker fork-join).** `tools/cook/cook_worker.cpp` — `std::vector<std::thread>` → `std::vector<gw::jobs::ReservedWorker>`, each spawned via `Scheduler::reserve_worker(worker)`. `next_idx.fetch_add` work-distribution lambda unchanged.
- **P2-3 (file watch abstraction).** Added `engine/platform/file_watch.{hpp,cpp}`:
  - Public type: `gw::platform::FileWatch` (pimpl, `add_watch` / `remove_watch` / `poll` / `is_supported`).
  - Implementation TU is the **only** place `<windows.h>` / `<sys/inotify.h>` appear for watcher work (matches the `dll.cpp` / `fs.cpp` single-TU convention in the same directory).
  - Windows path uses non-blocking `OVERLAPPED + WaitForSingleObject(…, 0) + CancelIo` so `poll()` honours its non-blocking contract across frames (the old `vfs/watcher.cpp` called the blocking form without overlap — fixed in passing).
  - `engine/assets/vfs/watcher.{hpp,cpp}` rewritten as a thin facade over `FileWatch`. `AssetOk` return surface preserved for editor/asset-db callers. No OS headers remain in `engine/assets/**`.
- **Audit map + daily log** updated: P1-1 / P2-1 / P2-2 / P2-3 rows moved to RESOLVED with commit-state annotations; this night-session block added.

### Verification
- `cmake --build --preset dev-win` — **green**.
- `ctest --preset dev-win` — **72 / 72 pass** (smoke + perf).
- Working tree still unstaged; no commit made (commits are only on user request).

### Round 1 status
All §7 Round-1 items are now RESOLVED. No Tier-G deferred obligations remain lurking; the sync1 + legacy-render-pass backend is only a real work item if/when cross-hardware CI (lavapipe, MoltenVK) gets added — tracked against the CI-matrix expansion task, not against ADR-0003.

### Late-night follow-up — ADR-0003 amendment
The first implementation of P1-1 left the frame-graph's barrier emit silently skipping `vkCmdPipelineBarrier2` when `!has_sync2()` — a correctness hole, not a fallback. Reviewed the options (full Tier-G sync1 backend vs. promoting sync2/dynamic_rendering to Tier-A) and promoted both to Tier-A:
- `VulkanDevice` now refuses to construct when either is missing, with a specific error pointing at the ADR.
- `FrameGraph::execute_pass` dropped the `has_sync2()` guard — the capability is a construction invariant.
- ADR-0003 §2.1 "Amendment" documents the change and the conditions under which we'd reopen it.
- `caps_` still surfaces Tier-B/Tier-G features (mesh shaders, ray tracing, work graphs, `ext_memory_budget`) — the capability layer is unchanged in shape, only the Tier-A boundary moved.
- Rebuild + ctest: 72/72 still green.

---

## 🌙 Late-night-II — Phase-7 fullstack Path-A kickoff (ECS-World)

**Session intent:** begin the ECS fullstack push (P1-11 fix). Doctrine first (ADRs 0004–0007) → ECS core → editor integration.

### Doctrine landed (read before code)
- `docs/adr/0004-ecs-world.md` — archetype-of-chunks World, 64-bit generational `Entity`, `ComponentRegistry`, `for_each<Cs...>`, dedicated `HierarchyComponent`, single-threaded, STL allowed in management paths (not hot paths).
- `docs/adr/0005-editor-command-stack.md` — editor-scoped undo/redo (`editor/undo/command_stack`), coalescing + grouping, 16 MiB budget.
- `docs/adr/0006-ecs-world-serialization.md` — binary `.gwblob`/`.gwscene` with zstd on by default, trivially-copyable fast path, reflection slow path, schema-evolution tolerance.
- `docs/adr/0007-bld-c-abi-freeze.md` — `BLD_ABI_VERSION` v1 frozen; Host→BLD and Plugin→Host surfaces enumerated; thread-safety contract (BLD may call `gw_editor_*` off-main; implementations queue to main).

### Decisions taken (user-confirmed)
- Hierarchy: **dedicated `HierarchyComponent`** (parent / first_child / next_sibling).
- ECS systems registry: **deferred to Phase 8+** (broken `SystemRegistry` deleted in this session).
- `CommandStack` location: **`editor/undo/`** (editor-scoped); the existing `engine/core/command.{hpp,cpp}` remains as a stepping stone until the editor-scoped one lands.
- Scene-file compression: **zstd on by default**.

### ECS core landed
- `engine/ecs/entity.hpp` rewritten: `struct Entity { uint64_t bits; }` with `{generation:32, index:32}` packing, `Entity::null()` / `from_parts()`, `std::hash<Entity>` specialization. `ComponentTypeId = uint16_t`, `kMaxComponentTypes = 256`. Old `uint32_t EntityHandle` + `ComponentType` / `ComponentFlags` enums deleted.
- `engine/ecs/component_registry.{hpp,cpp}`: runtime per-type info (size, align, trivially-copyable, stable FNV1a hash of `gw_type_name<T>()`, lifecycle hooks, optional `gw::core::TypeInfo*`). `id_of<T>()` is O(1) after first-call (per-T static hash cache).
- `engine/ecs/archetype.{hpp,cpp}` rewritten: `Chunk` (16 KiB default, SOA arrays packed by descending alignment), `Archetype` (owns N `Chunk`s, lazy growth, swap-back compaction on destroy), `ArchetypeTable` (mask → id lookup, `for_each_matching(required_mask, fn)`).
- `engine/ecs/world.{hpp,cpp}`: top-level `World` — `create_entity` / `destroy_entity` / `is_alive`, `add_component<T>` / `remove_component<T>` / `has_component<T>` / `get_component<T>`, `for_each<Cs...>(fn)` that dispatches archetype → chunk → slot, hierarchy helpers (`reparent` with cycle rejection, `visit_descendants` DFS, `visit_roots`, `children_of`). Entity migration handles trivial (`memcpy`) and non-trivial (move_construct + destruct) component paths.
- `engine/ecs/hierarchy.hpp`: `HierarchyComponent { parent, first_child, next_sibling }`.
- Deleted: `engine/ecs/component.{hpp,cpp}`, `engine/ecs/system.{hpp,cpp}`, `engine/ecs/chunk.{hpp,cpp}`, old `archetype.{hpp,cpp}`. All were stubs or broken (inverted `static_assert`, undefined `for_each`).

### Editor handle unification
- `editor/selection/selection_context.hpp`: `using EntityHandle = gw::ecs::Entity;` (was `uint64_t`). `kNullEntity = gw::ecs::Entity::null()`. All editor panels, commands, gizmo, viewport continue to compile under the `EntityHandle` alias; the few integer-literal call sites (`inspector_panel.cpp` `0x%llx` printf, `outliner_panel.cpp` fake-id minting, `editor_bld_api.cpp` BLD surface) now go through `entity.bits`.

### Tests
- `tests/unit/ecs/world_test.cpp` — 11 new cases: create/destroy, generation bump on reuse, add/get/has/remove, repeat-add overwrite, archetype migration preserves other components, `for_each` matches superset archetypes, non-trivially-copyable component lifecycle (`std::string` in `Name`), hierarchy reparent + DFS, cycle rejection (self-reparent + transitive), 10 000-entity stress with mixed archetypes + 1/3 destroy.
- `tests/unit/editor/selection_test.cpp` — ported to `Entity{N}` literals.

### Verification
- `cmake --build --preset dev-win` — **green**.
- `ctest --preset dev-win` — **83 / 83 pass** (up from 72; +11 ECS).

### Remaining Phase-7 fullstack work
Doctrine + ECS-core are done; editor integration and infrastructure are next. Tracked in the todo list:
1. **Scene bootstrap** — plug a real `gw::ecs::World` into `EditorContext.world` (replacing the `void*` placeholder); seed demo entities (cube, sphere, light) for the outliner/inspector to bind to.
2. **Outliner integration** — walk `World::visit_roots` + `World::visit_descendants`; reparent / create / delete write through to the real World.
3. **Inspector integration** — read components via `ComponentRegistry::info(id).reflection` → `WidgetDrawer::draw_field`; replace static `demo_transform`.
4. **Editor-scoped `CommandStack`** per ADR-0005 — `editor/undo/command_stack.{hpp,cpp}`; wire Ctrl+Z/Y bindings; coalesce rapid drags.
5. **Inspector write-through** via CommandStack.
6. **Viewport VMA offscreen RT** (the original Phase-7 week-040 deliverable).
7. **Debug-draw** (line/sphere/frustum).
8. **Asset-browser thumbnails + drag-to-scene**.
9. **ECS world serialization** per ADR-0006 (save/load + round-trip tests).
10. **PIE snapshot / restore** via serialization.
11. **BLD C-ABI** per ADR-0007 (`bld_register.h` + smoke wiring).
12. **Editor v0.1 demo recording**; flip `docs/05` Phase-7 row to `completed`, Phase-8 row to `in_progress`.

## 🔮 Next session

1. Continue Phase-7 fullstack Path-A: scene bootstrap → outliner/inspector integration → editor CommandStack → viewport RT.
2. Round-2 items (P1-6 / P2-11 / P2-6 / P2-7) remain deferred until Phase-7 closes.
