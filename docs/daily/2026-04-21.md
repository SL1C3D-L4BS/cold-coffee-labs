# 2026-04-21 · Tuesday

**Week:** 42 · **Phase:** Phase 7 — Editor Foundation · **Milestone:** *Editor v0.1*

---

## Today's Focus
Phase-7 fullstack Path-A gates C + D: ECS serialization per ADR-0006, PIE
snapshot/restore, and BLD C-ABI v1 freeze per ADR-0007.

## Tasks Completed
- [x] `engine/core/serialization.{hpp,cpp}`: `BinaryWriter` / `BinaryReader` +
      CRC-64/ISO primitives. Little-endian, trivially-copyable memcpy fast
      path, `u32`-prefixed strings.
- [x] `engine/ecs/serialize.{hpp,cpp}`: `save_world` / `load_world` /
      `save_entity` / `load_entity` with header + CRC framing, component
      table indexed by `stable_hash`, `std::expected<_, SerializationError>`
      error surface. Entity-major layout (deviates from ADR's
      archetype-major chunk-grouped layout — documented inline for a
      follow-up profiling pass).
- [x] `World::{clear, restore_entity_with_bits, add_component_raw}` to
      support the loader's reuse-bits path so inter-entity refs
      (`HierarchyComponent.parent`) survive round trip.
- [x] 8 serialize tests: world/entity round-trip, hierarchy refs, bad
      magic, CRC mismatch, CRC disable, truncated blob, empty world.
- [x] `engine/core/time.hpp`: `TimeState` POD (total/dt/frame/time_scale)
      crossed across the `GameplayContext` C-ABI via `void*`.
- [x] `editor/pie/gameplay_host.{hpp,cpp}`: `take_snapshot` /
      `restore_snapshot` wired through `ecs::save_world`/`load_world`.
      `enter_play` refuses transition on snapshot failure so the user
      never gets stranded. DLL load is optional when `lib_source_path` is
      empty — scene-only iteration works without a gameplay module.
- [x] `editor/app/editor_app.{hpp,cpp}`: Play/Pause/Stop menu +
      F5/Shift+F5/F6 keybinds; `TimeState` advanced each frame and surfaced
      through `pie_ctx_.time`.
- [x] 5 PIE tests: snapshot/restore round-trip, hierarchy preservation,
      enter_play→stop without a dll, null-world rejection, pause/resume.
- [x] Gate C committed as `7c8fd60` (Phase 7 Path-A gate C).
- [x] `bld/include/bld_register.h`: Surface-H v1 (`bld_abi_version`,
      `bld_register`, `bld_shutdown`, `bld_tick`) with opaque handles.
- [x] `editor/bld_api/editor_bld_api.{hpp,cpp}`: Surface-P v1 additions
      (`bld_registrar_register_tool`, `bld_registrar_register_widget`,
      `gw_editor_run_tool`, `gw_editor_log`, `gw_editor_log_error`).
      Registrar concrete type owns tool/widget tables + installable log
      sink.
- [x] `tests/smoke/bld_abi_v1/bld_abi_smoke_test.cpp`: 4 cases driving the
      full ADR-0007 §2.7 sequence (registrar → plugin register → run_tool →
      log sink) in-process. A cross-dylib harness is deferred to Phase 9 —
      every line of the body carries over.
- [x] Flipped `docs/05 §Editor v0.1` sign-off state to COMPLETED
      (engineering). Recording gate tracked separately per §0 rule.

## Blockers
Pre-existing `World — destroy_entity invalidates the handle; reuse bumps
generation` test failure (world_test.cpp:60) — generation resets to 0 then
climbs to 1, equal to original. Not in gate-C/D scope; filed for its own
followup.

## Notes / Log
- Entity-major serialization is pragmatic for today's scenes (all
  components trivially copyable; few dozen entities). ADR-prescribed
  archetype-major chunk layout can be added later behind the same
  `save_world`/`load_world` surface if profiling shows cache misses.
- In-process smoke test gives us ABI-shape lock without platform-specific
  loader plumbing. That plumbing is Phase-9 BLD's natural home — Corrosion
  already builds `bld_ffi` as a Rust crate; the same rig will host a real
  Surface-H dylib when BLD lands its first export.
- 101 doctest cases green (was 58 pre-Path-A).

## Tomorrow's Intention
Record the two-minute Editor v0.1 narrated demo against the now-green
stack, then pull the first Phase-8 card (`.gwscene` binary format with
migration hooks) — the ECS serializer from gate C is its underlying
engine.
