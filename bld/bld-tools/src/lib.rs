//! bld-tools — the `#[bld_tool]` proc-macro surface and tool registry.
//! Phase 9 wires the macro and autogenerates schemas. This is a Phase 1 stub.

#![warn(missing_docs)]

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        assert_eq!(2 + 2, 4);
    }
}
